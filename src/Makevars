## Use the R_HOME indirection to support installations of multiple R version
CXX_STD = CXX11

# PKG_CXXFLAGS = -std=c++11 -fopenmp -I../inst/include -isystem `$(R_HOME)/bin/Rscript -e 'cat(system.file("include", package="RcppEigen"))'` -isystem `$(R_HOME)/bin/Rscript -e 'cat(system.file("include", package="Rcpp"))'` -isystem `$(R_HOME)/bin/Rscript -e 'cat(system.file("include", package="BH"))'` -pedantic -Wall -Wextra
# PKG_LIBS = `$(R_HOME)/bin/Rscript -e "Rcpp:::LdFlags()"` -lgomp

PKG_CXXFLAGS =  -I../inst/include -I `$(R_HOME)/bin/Rscript -e 'cat(system.file("include", package="RcppEigen"))'` -I `$(R_HOME)/bin/Rscript -e 'cat(system.file("include", package="Rcpp"))'` -I `$(R_HOME)/bin/Rscript -e 'cat(system.file("include", package="BH"))'`

# TODO: check warnings with -Wno-sign-conversion

## As an alternative, one can also add this code in a file 'configure'
##
##    PKG_LIBS=`${R_HOME}/bin/Rscript -e "Rcpp:::LdFlags()"`
## 
##    sed -e "s|@PKG_LIBS@|${PKG_LIBS}|" \
##        src/Makevars.in > src/Makevars
## 
## which together with the following file 'src/Makevars.in'
##
##    PKG_LIBS = @PKG_LIBS@
##
## can be used to create src/Makevars dynamically. This scheme is more
## powerful and can be expanded to also check for and link with other
## libraries.  It should be complemented by a file 'cleanup'
##
##    rm src/Makevars
##
## which removes the autogenerated file src/Makevars. 
##
## Of course, autoconf can also be used to write configure files. This is
## done by a number of packages, but recommended only for more advanced users
## comfortable with autoconf and its related tools.


